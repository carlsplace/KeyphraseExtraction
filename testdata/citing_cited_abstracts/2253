A Simple Algorithm For Finding Frequent Elements In Streams And Bags We present a simple, exact algorithm for iceberg queries (identifying in a multidet the items with frequency more than a threshold \theta) that requires two passes, linear time, and space 1/ \theta .
